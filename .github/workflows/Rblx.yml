name: Latest Roblox Activity
on:
  schedule:
    - cron: '0 0 * * *' 
  workflow_dispatch: 
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write 
    steps:
      - uses: actions/checkout@v3
      - name: Fetch Roblox Profile
        uses: eclipsecool/Insta-pull-labs@v1
        with:
          roblox_user_id: '1714963905'
